import { headings } from './typography';

export const lightTheme = {
  bg: 'white.500',
  color: 'body.500',
};

export const darkTheme = {
  bg: 'dark.500',
  color: 'white.500',
};

const styles = {
  global: {
    ...headings,
  },
};

export default styles;
